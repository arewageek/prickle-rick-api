import supertest from "supertest";
import app from "../app";

describe("POST /users", () => {
  describe("createa a user account using telegram id", () => {
    // should create a user from given telegram id
    // should respond with a json object containing user data
    // should respond with a 200 status code indicating account creation was successful
    // should respond json in the content type header
  });
});
